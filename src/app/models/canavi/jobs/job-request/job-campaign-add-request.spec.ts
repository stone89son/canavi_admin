import { JobCampaignAddRequest } from './job-campaign-add-request';

describe('JobCampaignAddRequest', () => {
  it('should create an instance', () => {
    expect(new JobCampaignAddRequest()).toBeTruthy();
  });
});
