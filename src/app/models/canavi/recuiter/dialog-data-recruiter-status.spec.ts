import { DialogDataRecruiterStatus } from './dialog-data-recruiter-status';

describe('DialogDataRecruiterStatus', () => {
  it('should create an instance', () => {
    expect(new DialogDataRecruiterStatus()).toBeTruthy();
  });
});
