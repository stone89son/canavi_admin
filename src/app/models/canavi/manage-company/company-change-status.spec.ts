import { CompanyChangeStatus } from './company-change-status';

describe('CompanyChangeStatus', () => {
  it('should create an instance', () => {
    expect(new CompanyChangeStatus()).toBeTruthy();
  });
});
